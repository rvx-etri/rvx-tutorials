CROSS_COMPILE = ${RVX_COMPILER_BASIC}
RISCV_ARCH=rv32im
RISCV_ABI=ilp32
CFLAGS += -march=${RISCV_ARCH} -mabi=${RISCV_ABI}
LD_FLAGS += -march=${RISCV_ARCH} -mabi=${RISCV_ABI}


INCLUDES += -I${RVX_SSW_HOME}/core/biriscv
ASM_SRC += ${RVX_SSW_HOME}/core/biriscv/start.S
ifeq ($(USE_FREERTOS),true)
	ASM_SRC += ${RVX_SSW_HOME}/core/biriscv/portasm.S
else
	ASM_SRC += ${RVX_SSW_HOME}/core/biriscv/entry.S
endif

include ${RVX_SSW_HOME}/riscv/riscv_compile.mh
